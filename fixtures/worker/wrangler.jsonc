{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "diffbot-langchain-worker-fixture",
  "main": "src/index.ts",
  "compatibility_date": "2025-04-01",
  // Empty on purpose: this is the assertion that @diffbot/langchain needs no
  // Node compatibility shims to run in a Worker. Adding "nodejs_compat" here
  // would silently retire that guarantee.
  "compatibility_flags": []
}
